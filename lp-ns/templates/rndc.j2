# {{ ansible_managed }}
key "{{ ns.rndc.keyname }}" {
    algorithm {{ ns.rndc.algo | default('hmac-md5') }};
    secret "{{ ns.rndc.key | default() }}";
};

options {
    default-server  localhost;
    default-key     "{{ ns.rndc.keyname }}";
};
