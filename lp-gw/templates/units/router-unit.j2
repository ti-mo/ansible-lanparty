[Unit]
Description=Lanparty Router
Requires=network.target
After=network.target

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/bin/router start
ExecStop=/usr/bin/router stop

[Install]
WantedBy=multi-user.target
