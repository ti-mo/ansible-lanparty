# {{ ansible_managed }}
[Unit]
Description=TC ruleset provider
Requires=network.target
After=network.target

[Service]
Type=oneshot
RemainAfterExit=yes
EnvironmentFile=/etc/tc/%I
ExecStart=/usr/bin/tc-lp start %I $LINERATE
ExecStop=/usr/bin/tc-lp stop %I

[Install]
WantedBy=multi-user.target
